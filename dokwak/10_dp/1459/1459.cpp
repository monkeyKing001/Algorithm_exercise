#include <bits/stdc++.h>
#define ll long long
int dr[3] = {1, 0, 1};
int dc[3] = {0, 1, 1};

using namespace std;
int n, m;

int	main(int argc, char **argv)
{
  ios::sync_with_stdio(0);
  cin.tie(0);
  cout.tie(0);
  int max_distance = 1e12+7;
  cin >> n >> m;
  int step, step_diag; 
  cin >> step >> step_diag;
  ll total_step_cost = 0;
  while (n || m){
    if (step < step_diag){
      total_step_cost = 


    }
    else{
    }
  }
  return (0);
}

